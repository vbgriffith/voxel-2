<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>PROJECT: CYCLEBREAKER</title>

  <!-- ========================= -->
  <!-- META / PLATFORM SETTINGS -->
  <!-- ========================= -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="theme-color" content="#0d0f14" />
  <meta name="description" content="A mythic action roguelite about death, memory, and defiance." />
  <meta name="robots" content="noindex, nofollow" />

  <!-- ========================= -->
  <!-- FONTS -->
  <!-- ========================= -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;800&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">

  <!-- ========================= -->
  <!-- CORE STYLES (INLINE ONLY) -->
  <!-- ========================= -->
  <style>
    :root {
      --bg: #0d0f14;
      --bg-alt: #151824;
      --fg: #e6e6eb;
      --muted: #9aa0b2;
      --accent: #c03a2b;
      --gold: #d4b46a;
      --danger: #ff4d4d;

      --font-title: 'Cinzel', serif;
      --font-body: 'Inter', system-ui, sans-serif;

      --ui-scale: 1;
      --shake-intensity: 1;
      --flash-intensity: 1;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      user-select: none;
    }

    html, body {
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at top, #161a2a, var(--bg));
      color: var(--fg);
      font-family: var(--font-body);
      overflow: hidden;
    }

    body {
      display: flex;
      align-items: center;
      justify-content: center;
    }

    /* ========================= */
    /* GAME CANVAS */
    /* ========================= */
    #game-root {
      position: relative;
      width: 100%;
      height: 100%;
    }

    canvas {
      display: block;
      width: 100%;
      height: 100%;
      outline: none;
    }

    /* ========================= */
    /* LOADING SCREEN */
    /* ========================= */
    #loading-screen {
      position: absolute;
      inset: 0;
      background: linear-gradient(180deg, #0b0d15, #0f1220);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      z-index: 10;
      transition: opacity 0.8s ease;
    }

    #loading-screen.hidden {
      opacity: 0;
      pointer-events: none;
    }

    .title {
      font-family: var(--font-title);
      font-size: 42px;
      letter-spacing: 4px;
      margin-bottom: 16px;
      color: var(--gold);
      text-shadow: 0 0 20px rgba(212,180,106,0.25);
    }

    .subtitle {
      font-size: 14px;
      color: var(--muted);
      letter-spacing: 1px;
      margin-bottom: 40px;
    }

    .loading-bar {
      width: 320px;
      height: 6px;
      background: #1a1e33;
      overflow: hidden;
      border-radius: 4px;
    }

    .loading-bar-fill {
      height: 100%;
      width: 0%;
      background: linear-gradient(90deg, var(--accent), var(--gold));
      transition: width 0.3s ease;
    }

    /* ========================= */
    /* UI OVERLAY */
    /* ========================= */
    #ui-layer {
      position: absolute;
      inset: 0;
      pointer-events: none;
      z-index: 5;
    }

    /* ========================= */
    /* ACCESSIBILITY MENU */
    /* ========================= */
    #accessibility {
      position: absolute;
      bottom: 20px;
      right: 20px;
      background: rgba(10, 12, 20, 0.85);
      border: 1px solid #1f2340;
      padding: 12px 16px;
      border-radius: 6px;
      font-size: 12px;
      pointer-events: auto;
    }

    #accessibility h4 {
      font-family: var(--font-title);
      font-size: 12px;
      margin-bottom: 8px;
      color: var(--gold);
      letter-spacing: 1px;
    }

    #accessibility label {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      margin-bottom: 6px;
      color: var(--muted);
    }

    #accessibility input[type="range"] {
      width: 100px;
    }

    /* ========================= */
    /* DEBUG (DISABLED BY DEFAULT) */
    /* ========================= */
    #debug {
      position: absolute;
      top: 10px;
      left: 10px;
      font-size: 11px;
      color: #6cff6c;
      font-family: monospace;
      display: none;
      pointer-events: none;
    }
  </style>
</head>

<body>
  <div id="game-root">

    <!-- ========================= -->
    <!-- LOADING SCREEN -->
    <!-- ========================= -->
    <div id="loading-screen">
      <div class="title">CYCLEBREAKER</div>
      <div class="subtitle">Death is not an ending.</div>
      <div class="loading-bar">
        <div class="loading-bar-fill" id="loading-progress"></div>
      </div>
    </div>

    <!-- ========================= -->
    <!-- GAME CANVAS -->
    <!-- ========================= -->
    <canvas id="game-canvas" tabindex="0"></canvas>

    <!-- ========================= -->
    <!-- UI LAYER -->
    <!-- ========================= -->
    <div id="ui-layer"></div>

    <!-- ========================= -->
